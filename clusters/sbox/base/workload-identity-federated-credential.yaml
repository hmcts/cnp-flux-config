apiVersion: managedidentity.azure.com/v1api20220131preview
kind: FederatedIdentityCredential
metadata:
  name: ${NAMESPACE}-fic
  namespace: ${NAMESPACE}
spec:
  owner:
    name: ${NAMESPACE}-${ENVIRONMENT}-mi
  audiences:
    - api://AzureADTokenExchange
  issuer: https://uksouth.oic.prod-aks.azure.com/00000000-0000-0000-0000-000000000000/00000000-0000-0000-0000-000000000000/
  subject: system:serviceaccount:${NAMESPACE}:${NAMESPACE}