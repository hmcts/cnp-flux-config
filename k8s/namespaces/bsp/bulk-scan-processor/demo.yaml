apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: bulk-scan-processor
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.java: glob:pr-1779-*
    hmcts.github.com/prod-automated: disabled
spec:
  values:
    java:
      environment:
        #60 days in seconds
        SAS_TOKEN_VALIDITY: "5184000"
        STORAGE_URL: https://bulkscandemo.blob.core.windows.net
