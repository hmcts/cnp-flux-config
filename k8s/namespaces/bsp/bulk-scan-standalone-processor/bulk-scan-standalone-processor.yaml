# this chart for Exela to get SAS token, always keep it running in demo standalone
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: bulk-scan-standalone-processor
  namespace: bsp
spec:
  releaseName: bulk-scan-standalone-processor
bsp:
  blobstorage:
    enabled: false
