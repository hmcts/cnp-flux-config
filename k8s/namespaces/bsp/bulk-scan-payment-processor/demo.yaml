apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: bulk-scan-payment-processor
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.java: glob:pr-470-*
    hmcts.github.com/prod-automated: disabled
spec:
  values:
    java:
      environment:
        PAYMENTS_CONSUME_TIME_INTERVAL_MS: "9000000" # 150 minutes
