apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: docmosis
  annotations:
    filter.fluxcd.io/chart-image: glob:sandbox-*
spec:
  values:
    ingressHost: docmosis.sandbox.platform.hmcts.net
    image: hmctsprivate.azurecr.io/docmosis:sandbox-c02aed7
