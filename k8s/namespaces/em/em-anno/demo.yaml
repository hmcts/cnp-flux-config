apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: em-anno
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.java: glob:pr-455-*
    hmcts.github.com/prod-automated: disabled
spec:
  values:
    java:
      keyVaults:
        em-anno:
          secrets:
            - name: microservicekey-em-annotation-app
              alias: S2S_KEY
            - name: AppInsightsInstrumentationKey
              alias: APPINSIGHTS_INSTRUMENTATIONKEY
            - name: anno-POSTGRES-PASS
              alias: SPRING_DATASOURCE_PASSWORD
      environment:
        TEST_VAR: value1
      image: hmctspublic.azurecr.io/em/anno:pr-455-fed35ceb