apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: em-hrs-ingestor
spec:
  values:
    job:
      environment:
        MAX_FILES_TO_PROCESS: 500
      schedule: "0 * * * *"
      # Run on the hour, every hour
