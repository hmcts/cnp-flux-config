apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: em-icp
spec:
  values:
    nodejs:
      replicas: 0
      environment:
        IDAM_API_BASE_URL: https://idam-api.platform.hmcts.net
