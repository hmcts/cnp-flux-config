---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: media-viewer
spec:
  values:
    nodejs:
      environment:
        SLACK_CHANNEL: "em-dev-chat"
        SLACK_NOTIFY_SUCCESS: "true"
