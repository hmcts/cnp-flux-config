apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: em-hrs-api
spec:
  values:
    java:
      environment:
        SHAREE_LINK_TTL: 1 #ttl in hours
        JSON_CONSOLE_PRETTY_PRINT: true
        LOG_OUTPUT: single
        ROOT_LOGGING_LEVEL: DEBUG
        LOG_LEVEL_SPRING_WEB: DEBUG
        LOG_LEVEL_DM: DEBUG
        REFORM_LOG_LEVEL: DEBUG
