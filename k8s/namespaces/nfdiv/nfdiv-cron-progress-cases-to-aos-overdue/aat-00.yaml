apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: nfdiv-cron-progress-cases-to-aos-overdue
spec:
  values:
    job:
      args:
        - run
        - SystemProgressCasesToAosOverdueTask
      schedule: 0/10 * * * *
    global:
      jobKind: CronJob
