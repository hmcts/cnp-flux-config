apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: nfdiv-cron-system-progress-held-cases
spec:
  values:
    job:
      args:
        - run
        - SystemProgressHeldCasesTask
      schedule: "0/10 * * * *"
    global:
      jobKind: CronJob
