apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: div-da
spec:
  releaseName: div-da
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/div-da
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true