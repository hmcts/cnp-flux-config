apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: div-cms
spec:
  values:
    java:
      replicas: 2
      memoryRequests: "768Mi"
      cpuRequests: "1000m"
      memoryLimits: "3072Mi"
      cpuLimits: "2500m"
      readinessDelay: 45
      readinessTimeout: 5
      readinessPeriod: 15
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/div/cms:prod-41e037a-20220516170749 #{"$imagepolicy": "flux-system:div-cms"}
      environment:
        IDAM_API_BASEURL: "https://idam-api.perftest.platform.hmcts.net"
      aadIdentityName: divorce
      prometheus:
        enabled: true
