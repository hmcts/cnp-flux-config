apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: div-emca
spec:
  values:
    java:
      environment:
        IDAM_API_HEALTH_URI: https://idam-api.perftest.platform.hmcts.net/health
        IDAM_API_URL: https://idam-api.perftest.platform.hmcts.net
        TRIG_VA: perftest-trig1
      prometheus:
        enabled: true
