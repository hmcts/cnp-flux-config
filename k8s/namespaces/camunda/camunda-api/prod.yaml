apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: camunda-api
spec:
  values:
    java:
      environment:
        WA_AUTO_CONFIGURE_TASKS_ENABLED: true
        TASK_MANAGEMENT_API_URL: http://wa-task-configuration-api-prod.service.core-compute-prod.internal
        TASK_CONFIGURATION_URL_PATH: task

