apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: lau-idam-backend
spec:
  releaseName: lau-idam-backend
  values:
    java:
      replicas: 3
      memoryRequests: "1024Mi"
      cpuRequests: "1000m"
      memoryLimits: "4096Mi"
      cpuLimits: "4000m"
