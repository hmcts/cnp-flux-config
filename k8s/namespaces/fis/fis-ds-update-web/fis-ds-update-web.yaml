apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: fis-ds-update-web
  namespace: fis
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.nodejs: glob:prod-*
spec:
  releaseName: fis-ds-update-web
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/fis-ds-update-web
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/fis/ds-update-web:prod-fe34ca7-20220706174156 #{"$imagepolicy": "flux-system:fis-ds-web"}
