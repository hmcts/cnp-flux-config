apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: fis-cos-api
  namespace: fis
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.nodejs: glob:prod-*
spec:
  releaseName: fis-cos-api
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/fis-cos-api
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/fis/cos-api:prod-181a903-20220331113745 #{"$imagepolicy": "flux-system:fis-cos-api"}
