apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: ds-ui
  namespace: ds
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.nodejs: glob:prod-*
spec:
  releaseName: ds-ui
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/ds-ui
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/ds/web:prod-9eac446-20220223165757 #{"$imagepolicy": "flux-system:ds-ui"}
