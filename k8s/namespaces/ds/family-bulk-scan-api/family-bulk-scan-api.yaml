apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: family-bulk-scan-api
  namespace: ds
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.nodejs: glob:prod-*
spec:
  releaseName: family-bulk-scan-api
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/family-bulk-scan-api
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/ds/family-bulk-scan-api:prod-2f477bb-20220316134520 #{"$imagepolicy": "flux-system:family-bulk-scan-api"}
