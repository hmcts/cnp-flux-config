apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: ccd-logstash
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  releaseName: ccd-logstash
  values:
    replicas: 0
