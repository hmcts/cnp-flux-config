apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer
  namespace: ccd

spec:
  releaseName: ccd-logstash-indexer
  values:
    java:
      replicas: 1
      environment:
        DUMMY_VAR: true
