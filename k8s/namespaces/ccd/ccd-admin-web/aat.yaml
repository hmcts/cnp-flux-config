apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: ccd-admin-web
spec:
  releaseName: ccd-admin-web
  values:
    nodejs:
      environment:
        ADMINWEB_ELASTIC_INDEX_URL: http://ccd-definition-store-api-aat.service.core-compute-aat.internal/elastic-support/index
