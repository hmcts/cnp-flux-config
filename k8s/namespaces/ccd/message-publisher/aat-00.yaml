---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: message-publisher
spec:
  values:
    java:
      environment:
        CCD_CASE_EVENTS_ENABLED: true
        SPRING_PROFILES_ACTIVE: dev # Delete env var when ASB exists on AAT
