apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: bar-web
spec:
  releaseName: bar-web
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/bar-web
  values:
    nodejs:
      replicas: 2
      image: hmctspublic.azurecr.io/bar/web:prod-6ccb44b-20220608165010 #{"$imagepolicy": "flux-system:bar-web"}
