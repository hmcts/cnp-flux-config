apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: am-role-assignment-refresh-batch
spec:
  values:
    job:
      schedule: "00 13 * * *"
      environment:
        AM_INFO: true
        DELETE_ORG: true
