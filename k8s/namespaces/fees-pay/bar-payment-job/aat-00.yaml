apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: bar-csv-report
  namespace: fees-pay
spec:
  releaseName: bar-csv-report
  values:
    job:
      environment:
        DUMMY_RESTART_VAR: false
    global:
      environment: aat
