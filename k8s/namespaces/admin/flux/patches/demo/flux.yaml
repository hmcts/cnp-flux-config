---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    memcached:
      tag: 1.6.6
    git:
      email: "hmcts-flux-demo@hmcts.net"
      user: "Flux demo"
