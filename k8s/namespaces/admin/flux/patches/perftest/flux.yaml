---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-test@HMCTS.NET"
      user: "Flux test"
    registry:
      includeImage: "none"