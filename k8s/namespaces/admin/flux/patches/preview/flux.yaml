---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    memcached:
      tag: 1.6.6
    git:
      email: "flux-dev@hmcts.net"
      user: "Flux DEV"
