---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-dev@hmcts.net"
      user: "Flux DEV"
    registry:
      includeImage: "none"