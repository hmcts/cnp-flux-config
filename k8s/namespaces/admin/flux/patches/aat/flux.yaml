---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-aat@hmcts.net"
      user: "Flux AAT"
    syncGarbageCollection:
      enabled: false
    registry:
      includeImage: "none"