---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    memcached:
      nodeSelector:
        agentpool: adminpool
      tolerations:
        - key: "pods"
          operator: "Equal"
          value: "admin"
          effect: "NoSchedule"
    nodeSelector:
      agentpool: adminpool
    tolerations:
      - key: "pods"
        operator: "Equal"
        value: "admin"
        effect: "NoSchedule"
    git:
      path: "k8s/aat/common,k8s/aat/cluster-01,k8s/aat/cluster-01-overlay,k8s/common"
      label: "aat-cluster-01"
