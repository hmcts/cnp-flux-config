---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  chart:
    version: 1.5.0
  values:
    git:
      email: "flux-prod@hmcts.net"
      user: "HMCTS Flux Prod"
