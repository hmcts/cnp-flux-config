---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-prod@hmcts.net"
      user: "HMCTS Flux Prod"
    registry:
      includeImage: "none"