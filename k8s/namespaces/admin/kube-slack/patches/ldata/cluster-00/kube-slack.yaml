---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: kube-slack
  namespace: admin
spec:
  values:
    envVars:
      SLACK_CHANNEL: "aks-monitor-ldata"
      SLACK_USERNAME: ldata-00-aks
