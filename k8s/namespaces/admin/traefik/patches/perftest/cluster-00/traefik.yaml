apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: traefik
  namespace: admin
spec:
  chart:
    version: 1.86.2
  values:
    imageTag: 1.7.24
    gzip:
      enabled: false
    loadBalancerIP: 10.10.41.250
    dashboard:
      domain: traefik00.service.core-compute-perftest.internal
    service:
      annotations:
        service.beta.kubernetes.io/azure-load-balancer-resource-group: aks-infra-perftest-rg
