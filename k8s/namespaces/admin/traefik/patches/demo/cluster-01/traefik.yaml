apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: traefik
  namespace: admin
spec:
  values:
    loadBalancerIP: 10.50.80.250
    kubernetes:
      ingressEndpoint:
        ip: 51.140.0.75
        useDefaultPublishedService: false
    forwardedHeaders:
      enabled: true
      trustedIPs:
        - 0.0.0.0
    dashboard:
      domain: traefik01.demo.platform.hmcts.net
    service:
      annotations:
        service.beta.kubernetes.io/azure-load-balancer-resource-group: aks-infra-demo-rg