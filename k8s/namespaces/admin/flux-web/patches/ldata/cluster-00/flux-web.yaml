---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux-web
  namespace: admin
  annotations:
    flux.weave.works/automated: "false"
spec:
  values:
    frontend:
      env:
        BASE_URL: //flux-web-00.service.core-compute-ethosldata.internal
        API_EXTERNAL_URL: //flux-web-00.service.core-compute-ethosldata.internal/api/v1
        WS_URL: wss://flux-web-00.service.core-compute-ethosldata.internal/ws/v1
    ingress:
      hosts:
        - host: flux-web-00.service.core-compute-ethosldata.internal
          paths:
            - frontend: /
            - backend: /api
            - backend: /ws
