---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: flux-helm-operator-ro
rules:
  - apiGroups: ['*']
    resources: ['*']
    verbs: ['*']

  - nonResourceURLs: ['*']
    verbs: ['*']
