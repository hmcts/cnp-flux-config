apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: dm-store-cdam
spec:
  values:
    java:
      environment:
        ENABLE_TTL: 'false'
        ENABLE_METADATA_MIGRATION: 'false'
        ENABLE_OVERRIDE_METADATA: 'false'
