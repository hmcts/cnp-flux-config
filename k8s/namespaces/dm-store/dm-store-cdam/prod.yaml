apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: dm-store-cdam
spec:
  values:
    java:
      replicas: 1
      environment:
        ENABLE_TTL: 'false'
        ENABLE_METADATA_MIGRATION: 'true'
        ENABLE_OVERRIDE_METADATA: 'false'
