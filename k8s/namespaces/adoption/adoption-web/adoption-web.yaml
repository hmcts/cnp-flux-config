apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: adoption-web
spec:
  releaseName: adoption-web
  rollback:
    enable: true
    retry: true
  chart:
    git: git@github.com:hmcts/hmcts-charts
    ref: master
    path: stable/adoption-web
  values:
    nodejs:
      replicas: 2
      readinessDelay: 45
      readinessTimeout: 5
      readinessPeriod: 15
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/adoption/web:prod-c013b59-20220317111507 #{"$imagepolicy": "flux-system:adoption-web"}
      # ingressHost: adoption-web.aat.platform.hmcts.net
