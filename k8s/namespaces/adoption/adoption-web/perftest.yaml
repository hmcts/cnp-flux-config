apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: adoption-web
  namespace: adoption
spec:
  releaseName: adoption-web
  interval: 1m
  values:
    nodejs:
      environment:
        PCQ_ENABLED: "true"
