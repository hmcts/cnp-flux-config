apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: rpe-send-letter-service
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.java: glob:pr-1726-*
    hmcts.github.com/prod-automated: disabled
spec:
  values:
    java:
      environment:
        ENCRYPTION_ENABLED: "false"
        REFRESH_PODS: "yes"
