apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: nfdiv-frontend
  namespace: nfdiv
spec:
  rules:
  - host: www.apply-divorce.service.gov.uk
    http:
      paths:
      - backend:
          serviceName: nfdiv-frontend
          servicePort: 80
        path: /
