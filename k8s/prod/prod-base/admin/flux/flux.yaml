---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-prod@hmcts.net"
      user: "HMCTS Flux Prod"
    helmOperator:
      tls:
        enable: true