apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: adoption
bases:
- ../../../namespaces/adoption
patches:
- target:
    group: helm.fluxcd.io
    version: v1
    kind: HelmRelease
    name: adoption-web
  path: adoption-web-patch.yaml
