apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../../namespaces/fees-pay/ccpay-bar-payment-job/bar-payment-job.yaml
patchesStrategicMerge:
  #flux patches
  - ../../namespaces/fees-pay/ccpay-bar-payment-job/aat.yaml

patches:
  - path: ../aat-helmrelease.yaml
    target:
      kind: HelmRelease
      annotationSelector: hmcts.github.com/global-defaults == enabled