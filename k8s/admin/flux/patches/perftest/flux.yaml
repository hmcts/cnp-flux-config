---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-test@HMCTS.NET"
      user: "Flux test"
    helmOperator:
      tls:
        enable: true