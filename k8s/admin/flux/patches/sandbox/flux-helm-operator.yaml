---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux-helm-operator
  namespace: admin
  annotations:
    flux.weave.works/automated: "false"
spec:
  values:
    tls:
      enable: false