---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "hmcts-flux-sandbox@hmcts.net"
      user: "Flux sandbox"
      branch: movekustomizedir
    prometheus:
      enabled: true