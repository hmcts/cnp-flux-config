---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: flux
  namespace: admin
spec:
  values:
    git:
      email: "flux-aat@hmcts.net"
      user: "Flux AAT"
    helmOperator:
      tls:
        enable: true