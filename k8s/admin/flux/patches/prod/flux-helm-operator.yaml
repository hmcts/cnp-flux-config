---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flux-helm-operator
  namespace: admin
  annotations:
    flux.weave.works/automated: "false"
spec:
  values:
    helm:
      versions: "v2,v3"
    tls:
      enable: true