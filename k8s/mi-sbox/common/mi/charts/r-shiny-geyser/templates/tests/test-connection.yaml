apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "r-shiny-geyser.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "r-shiny-geyser.name" . }}
    helm.sh/chart: {{ include "r-shiny-geyser.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "r-shiny-geyser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
