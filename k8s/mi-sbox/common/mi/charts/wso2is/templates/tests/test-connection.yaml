apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wso2is.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "wso2is.name" . }}
    helm.sh/chart: {{ include "wso2is.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "wso2is.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
