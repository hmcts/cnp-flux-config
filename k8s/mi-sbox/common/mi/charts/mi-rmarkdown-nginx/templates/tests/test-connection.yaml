apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-rmarkdown-nginx.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "mi-rmarkdown-nginx.name" . }}
    helm.sh/chart: {{ include "mi-rmarkdown-nginx.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mi-rmarkdown-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
