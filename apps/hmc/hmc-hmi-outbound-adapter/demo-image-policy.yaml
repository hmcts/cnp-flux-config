apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: demo-hmc-hmi-outbound-adapter
spec:
  filterTags:
    pattern: '^pr-207-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: hmc-hmi-outbound-adapter
