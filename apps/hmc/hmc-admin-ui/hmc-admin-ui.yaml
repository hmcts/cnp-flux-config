apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hmc-admin-ui
spec:
  releaseName: hmc-admin-ui
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/hmc/admin-ui:prod-e305f56-20260130160303 #{"$imagepolicy": "flux-system:hmc-admin-ui"}
  chart:
    spec:
      chart: ./stable/hmc-admin-ui
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
