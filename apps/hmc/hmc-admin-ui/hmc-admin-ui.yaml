apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hmc-admin-ui
spec:
  releaseName: hmc-admin-ui
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/hmc/admin-ui:prod-59b27a0-20260902140648 #{"$imagepolicy": "flux-system:hmc-admin-ui"}
  chart:
    spec:
      chart: ./stable/hmc-admin-ui
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
