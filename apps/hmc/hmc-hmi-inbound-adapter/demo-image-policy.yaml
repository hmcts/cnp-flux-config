apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: demo-hmc-hmi-inbound-adapter
spec:
  filterTags:
    pattern: '^pr-176-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
    policy:
      alphabetical:
        order: asc
  imageRepositoryRef:
    name: hmc-hmi-inbound-adapter
