apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hmc-hmi-inbound-adapter
spec:
  values:
    java:
      environment:
        DUMMY_VAR: 0
      replicas: 2
      autoscaling:
        enabled: false
      memoryRequests: '1Gi'
