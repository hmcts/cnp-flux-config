apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hmc-hmi-inbound-adapter
spec:
  values:
    java:
      replicas: 2
      autoscaling:
        enabled: true
        maxReplicas: 4
      memoryRequests: '1024Mi'
      memoryLimits: '1536Mi'
      cpuRequests: '100m'
      cpuLimits: '250m'