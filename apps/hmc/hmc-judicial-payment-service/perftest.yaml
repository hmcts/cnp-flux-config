apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hmc-judicial-payment-service
spec:
  values:
    java:
      replicas: 0
      environment:

      memoryRequests: '1Gi'
