apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apim-marketplace-examples
spec:
  releaseName: apim-marketplace-examples
  values:
    java:
      image: hmctsprod.azurecr.io/apim/marketplace-examples:latest #{"$imagepolicy": "flux-system:apim-marketplace-examples"}
  chart:
    spec:
      chart: ./stable/apim-marketplace-examples
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
