apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: apim-marketplace
spec:
  imageRepositoryRef:
    name: apim-marketplace
  filterTags:
    pattern: '^prod-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
