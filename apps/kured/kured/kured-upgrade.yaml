apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kured
  namespace: kured
spec:
  chart:
    spec:
      chart: kured
      version: 4.4.2
      sourceRef:
        kind: HelmRepository
        name: kured
        namespace: kured