apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tfplan-viewer
  namespace: tfplan-viewer
spec:
  releaseName: tfplan-viewer
  values:
    environment:
      AZURE_STORAGE_ACCOUNT: ekterraformparser
  chart:
    spec:
      chart: nodejs
      version: 3.2.0
      sourceRef:
        kind: HelmRepository
        name: hmctspublic-oci
        namespace: flux-system
