apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-admin-frontend
spec:
  releaseName: fact-admin-frontend
  values:
    nodejs:
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/fact/admin-frontend:prod-97b9edb-20260907081100 #{"$imagepolicy": "flux-system:fact-admin-frontend"}
  chart:
    spec:
      chart: ./stable/fact-admin-frontend
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
