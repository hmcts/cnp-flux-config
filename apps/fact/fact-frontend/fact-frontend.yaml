apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-frontend
spec:
  releaseName: fact-frontend
  values:
    nodejs:
      memoryRequests: "650Mi"
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/fact/frontend:prod-4f9939b-20260319110031 #{"$imagepolicy": "flux-system:fact-frontend"}
  chart:
    spec:
      chart: ./stable/fact-frontend
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
