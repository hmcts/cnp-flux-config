apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-data-api
spec:
  values:
    java:
      environment:
        TESTING_SUPPORT_ENABLE_API: true
        TESTING_SUPPORT_ENABLE_CACHE: true
