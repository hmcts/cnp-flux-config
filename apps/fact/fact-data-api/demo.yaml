apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-data-api
spec:
  values:
    java:
      environment:
        TESTING_SUPPORT_ENABLE_API: true
        FACT_MIGRATION_SOURCE_BASE_URL: http://fact-api-prod.service.core-compute-prod.internal
