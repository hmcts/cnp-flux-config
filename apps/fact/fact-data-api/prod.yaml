apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-data-api
spec:
  values:
    java:
      ingressHost: fact-data-api.platform.hmcts.net
      environment:
        FACT_MIGRATION_SOURCE_BASE_URL: http://fact-api-prod.service.core-compute-prod.internal