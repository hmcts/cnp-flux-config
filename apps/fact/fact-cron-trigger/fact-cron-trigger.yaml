apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-cron-trigger
spec:
  releaseName: fact-cron-trigger
  values:
    job:
      image: hmctspublic.azurecr.io/fact/cron-trigger:prod-7300524-20250410102826 # {"$imagepolicy": "flux-system:fact-cron-trigger"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/fact-cron-trigger
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
