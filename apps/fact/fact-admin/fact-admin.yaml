apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-admin
spec:
  releaseName: fact-admin
  values:
    nodejs:
      replicas: 2
      memoryRequests: "720Mi"
      memoryLimits: "1024Mi"
      cpuRequests: "100m"
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/fact/admin:prod-b4a52a7-20260414144452 #{"$imagepolicy": "flux-system:fact-admin"}
      environment:
        VAR_TA: trigger18
  chart:
    spec:
      chart: ./stable/fact-admin
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
