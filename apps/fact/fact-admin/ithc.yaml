apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-admin
spec:
  values:
    nodejs:
      imagePullPolicy: Always
      environment:
        HMCTS_ACCESS_URL: 'https://hmcts-access.{{ .Values.global.environment }}.platform.hmcts.net'
