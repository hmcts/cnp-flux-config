apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-admin
spec:
  values:
    nodejs:
      image: hmctspublic.azurecr.io/fact/admin:pr-902-9835d44-20250912085024
      fact:
        secrets:
          - name: user-lock-timeout
            alias: USER_LOCK_TIMEOUT
          - name: oauth-client-secret
            alias: OAUTH_CLIENT_SECRET
          - name: redis-access-key
            alias: REDIS_ACCESS_KEY
          - name: AppInsightsInstrumentationKey-ai
            alias: APPINSIGHTS_INSTRUMENTATIONKEY
          - name: csrf-token-secret
            alias: CSRF_TOKEN_SECRET
          - name: launchdarkly-sdk-key
            alias: LAUNCHDARKLY_SDK_KEY
          - name: storage-account-connection-string
            alias: STORAGE_ACCOUNT_CONNECTION_STRING
          - name: storage-account-name
            alias: STORAGE_ACCOUNT_NAME
          - name: storage-account-primary-key
            alias: STORAGE_ACCOUNT_PRIMARY_KEY