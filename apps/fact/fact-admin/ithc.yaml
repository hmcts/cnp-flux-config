apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fact-admin
spec:
  values:
    nodejs:
      image: hmctspublic.azurecr.io/fact/admin:pr-901-a912e52-20250911085719
      fact:
        secrets:
          - user-lock-timeout
          - oauth-client-secret
          - redis-access-key
          - AppInsightsInstrumentationKey-ai
          - csrf-token-secret
          - launchdarkly-sdk-key
          - storage-account-connection-string
          - storage-account-name
          - storage-account-primary-key