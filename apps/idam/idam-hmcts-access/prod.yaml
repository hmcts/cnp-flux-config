apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: idam-hmcts-access
  namespace: idam
spec:
  releaseName: idam-hmcts-access
  values:
    nodejs:
      ingressHost: hmcts-access.platform.hmcts.net
      replicas: 2
      environment:
        IDAM_API_BASE_URL: 'https://idam-api.platform.hmcts.net'
