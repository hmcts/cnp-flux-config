apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: idam-hmcts-access
  namespace: idam
spec:
  releaseName: idam-hmcts-access
  values:
    nodejs:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/idam/hmcts-access:prod-25d34ad-20260908090658 #{"$imagepolicy": "flux-system:idam-hmcts-access"}
      memoryRequests: "1536Mi"
      memoryLimits: "3072Mi"
      cpuRequests: "500m"
      cpuLimits: "1500m"
      environment:
        PASSWORD_RESET_V2_ENABLED: true
  chart:
    spec:
      chart: ./stable/idam-hmcts-access
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
