apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: idam-user-dashboard
  namespace: idam
spec:
  releaseName: idam-user-dashboard
  values:
    nodejs:
      replicas: 1
      ingressHost: idam-user-dashboard.service.core-compute-preview.internal
      environment:
        STRATEGIC_SERVICE_URL: 'http://idam-api.preview.platform.hmcts.net'
        STRATEGIC_PUBLIC_URL: 'http://idam-web-public.preview.platform.hmcts.net'
        DASHBOARD_URL: 'http://idam-user-dashboard.preview.platform.hmcts.net'
        OAUTH_CLIENT_REDIRECT: 'http://idam-user-dashboard.preview.platform.hmcts.net/oauth2/callback'
