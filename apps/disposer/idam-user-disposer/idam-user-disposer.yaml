apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: idam-user-disposer
spec:
  releaseName: idam-user-disposer
  values:
    job:
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/disposer/idam-user-disposer:prod-d7dc5ca-20230629171457 #{"$imagepolicy": "flux-system:idam-user-disposer"}
  chart:
    spec:
      chart: ./stable/idam-user-disposer
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
