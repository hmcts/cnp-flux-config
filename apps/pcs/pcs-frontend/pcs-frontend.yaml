apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pcs-frontend
spec:
  releaseName: pcs-frontend
  values:
    nodejs:
      replicas: 2
      image: hmctsprod.azurecr.io/pcs/frontend:prod-a3c6dd9-20260708093210 #{"$imagepolicy": "flux-system:pcs-frontend"}
      environment:
        TRIGGER_VAR: 1
  chart:
    spec:
      chart: ./stable/pcs-frontend
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
