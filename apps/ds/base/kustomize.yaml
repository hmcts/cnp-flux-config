apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: ds
  namespace: flux-system
spec:
  path: ./apps/ds/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "ds"
      TEAM_NOTIFICATION_CHANNEL: "family-integration-cicd-notice"
