apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rpe-service-auth-provider
spec:
  releaseName: rpe-service-auth-provider
  values:
    java:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/rpe/service-auth-provider:prod-d48ffaa-20260827102840 #{"$imagepolicy": "flux-system:rpe-service-auth-provider"}
      environment:
        DUMMY_RESTART_VAR: 2
  chart:
    spec:
      chart: ./stable/rpe-service-auth-provider
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
