apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: nfdiv-cron-remind-awaiting-joint-final-order
spec:
  imageRepositoryRef:
    name: nfdiv-cron-remind-awaiting-joint-final-order
