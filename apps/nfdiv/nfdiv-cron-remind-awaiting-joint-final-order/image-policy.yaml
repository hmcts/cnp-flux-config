apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: nfdiv-cron-remind-awaiting-joint-final-order
spec:
  imageRepositoryRef:
    name: nfdiv-cron-remind-awaiting-joint-final-order
