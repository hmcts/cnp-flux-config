apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: bar-web
spec:
  releaseName: bar-web
  values:
    nodejs:
      replicas: 2
      image: hmctspublic.azurecr.io/bar/web:prod-dfbd132-20231211103758 #{"$imagepolicy": "flux-system:bar-web"}
  chart:
    spec:
      chart: ./stable/bar-web
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
