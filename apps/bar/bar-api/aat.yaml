apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bar-api
spec:
  releaseName: bar-api
  values:
    java:
      replicas: 1
      autoscaling:
        enabled: false
      environment:
        DUMMY_RESTART_VAR: true
