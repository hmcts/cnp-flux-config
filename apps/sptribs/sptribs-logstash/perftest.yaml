apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sptribs-logstash
  namespace: sptribs
spec:
  releaseName: sptribs-logstash
  values:
    replicas: 1
    extraEnvs:
      - name: LS_LOG_LEVEL
        value: debug
