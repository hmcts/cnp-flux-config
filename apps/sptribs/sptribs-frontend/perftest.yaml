apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: sptribs-frontend
spec:
  values:
    nodejs:
      environment:
        PCQ_ENABLED: false
        VAR_TA: perf-1
      autoscaling:
        enabled: true
        maxReplicas: 4
