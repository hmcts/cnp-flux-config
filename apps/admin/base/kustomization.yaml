apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ../kube-slack/kube-slack.yaml
  - ../traefik/traefik.yaml

patchesStrategicMerge: 
  - ../traefik/${ENVIRONMENT}/{CLUSTER}.yaml
