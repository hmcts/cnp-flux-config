apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: traefik
  namespace: admin
spec:
  ##################################
  # TODO

  # Is this file needed as a patch in demo??
  ##################################
  values:
    ssl:
      enabled: true
    debug:
      enabled: true