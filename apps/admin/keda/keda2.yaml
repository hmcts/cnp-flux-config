---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: keda
  namespace: admin
spec:
  values:
    podIdentity:
      activeDirectory:
        identity: keda
  chart:
    spec:
      chart: keda
      version: 2.6.0