---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: keda-2
  namespace: admin
spec:
  releaseName: keda-2
  values:
    image:
      pullPolicy: IfNotPresent
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
    resources:
      limits:
        cpu: 500m
        memory: 1024Mi
      requests:
        cpu: 250m
        memory: 256Mi
    aadPodIdentity: keda
  chart:
    spec:
      chart: keda
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: keda
        namespace: admin

