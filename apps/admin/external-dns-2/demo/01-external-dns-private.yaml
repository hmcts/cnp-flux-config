apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns-private-2
  namespace: admin
spec:
  releaseName: external-dns-private
  values:
    logLevel: debug
    txtOwnerId: cft-demo-aks-active