apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adoption-web
spec:
  releaseName: adoption-web
  interval: 1m
  values:
    nodejs:
      cpuLimits: "2000m"
      cpuRequests: '1'
      environment:
        PCQ_ENABLED: "true"
