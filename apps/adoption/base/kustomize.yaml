apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: adoption
  namespace: flux-system
spec:
  path: ./apps/adoption/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "adoption"
      TEAM_NOTIFICATION_CHANNEL: "fpla_adoption_tech"
