apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: camunda-optimize
spec:
  values:
    environment:
      OPTIMIZE_ELASTICSEARCH_HOST: https://platops-elastic-poc.kb.uksouth.azure.elastic-cloud.com
      OPTIMIZE_ELASTICSEARCH_HTTP_PORT: 9243
    replicas: 1
  valuesFrom:
  - name: "elastic-values"
    kind: Secret
