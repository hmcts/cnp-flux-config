apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: camunda-optimize
spec:
  values:
    environment:
      OPTIMIZE_ELASTICSEARCH_HOST: platops-elastic-poc.kb.uksouth.azure.elastic-cloud.com:9243
    replicas: 1
  valuesFrom:
  - name: "elastic-values"
    kind: Secret
