apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: camunda-optimize
spec:
  values:
    environment:
      OPTIMIZE_ELASTICSEARCH_HOST: camunda-elastic-cloud-sandbox.es.uksouth.azure.elastic-cloud.com
