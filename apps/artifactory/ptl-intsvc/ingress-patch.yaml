apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: artifactory-oss
  namespace: artifactory
spec:
  values:
    artifactory:
      ingress:
        hosts:
          - artifactory.platform.hmcts.net