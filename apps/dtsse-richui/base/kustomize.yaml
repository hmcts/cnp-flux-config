apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dtsse-richui
  namespace: flux-system
spec:
  path: ./apps/dtsse-richui/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "dtsse-richui"
      TEAM_NOTIFICATION_CHANNEL: "rse-dev-tools-builds"
