apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: aso
  namespace: flux-system
spec:
  path: ./apps/aso/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "aso"
      TEAM_NOTIFICATION_CHANNEL: "${ENV_MONITOR_CHANNEL}"
