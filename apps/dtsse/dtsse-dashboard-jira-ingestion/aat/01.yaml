apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dtsse-dashboard-jira-ingestion
spec:
  releaseName: dtsse-dashboard-jira-ingestion
  values:
    job:
      schedule: "10,30,50 * * * *"
    global:
      jobKind: CronJob
