apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-back-office-int
spec:
  releaseName: probate-back-office-int
  values:
    java:
      replicas: 2
      memoryRequests: "1.5Gi"
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/probate/back-office:prod-8c77ec7-20250626104817
      environment:
        # Does what it says on the tin - if this changes then all envs should redeploy
        CHANGE_TO_REDEPLOY: abcd
  chart:
    spec:
      chart: ./stable/probate-back-office
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
