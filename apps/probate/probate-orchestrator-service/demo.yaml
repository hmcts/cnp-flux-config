apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-orchestrator-service
  namespace: probate
spec:
  values:
    java:
      image: hmctspublic.azurecr.io/probate/orchestrator-service:prod-c4d59c2-20260227113533 #{"$imagepolicy": "flux-system:demo-probate-orchestrator-service"}
      ingressHost: probate-orchestrator-service-demo.service.core-compute-demo.internal
      environment:
        # Does what it says on the tin - if this changes then demo should redeploy
        CHANGE_TO_REDEPLOY_DEMO: 20251215
