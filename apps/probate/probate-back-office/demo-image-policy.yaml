apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: demo-probate-back-office
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  filterTags:
    # this image has a non-standard BackOffice and Caveats ccd definition.
    pattern: '^pr-3683-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: probate-back-office
