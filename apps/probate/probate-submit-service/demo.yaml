apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-submit-service
  namespace: probate
spec:
  values:
    java:
      image: hmctsprod.azurecr.io/probate/submit-service:prod-db78b9a-20260506081630 #{"$imagepolicy": "flux-system:demo-probate-submit-service"}
      environment:
        # Does what it says on the tin - if this changes then demo should redeploy
        CHANGE_TO_REDEPLOY_DEMO: 20251215
