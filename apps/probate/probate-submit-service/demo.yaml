apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-submit-service
  namespace: probate
spec:
  values:
    java:
      image: hmctspublic.azurecr.io/probate/submit-service:prod-43310b4-20260203125504 #{"$imagepolicy": "flux-system:demo-probate-submit-service"}
      environment:
        # Does what it says on the tin - if this changes then demo should redeploy
        CHANGE_TO_REDEPLOY_DEMO: 20251215
