apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-submit-service
  namespace: probate
spec:
  values:
    java:
      image: hmctsprod.azurecr.io/probate/submit-service:prod-cc1f7e1-20260825104802 #{"$imagepolicy": "flux-system:demo-probate-submit-service"}
      environment:
        # Does what it says on the tin - if this changes then demo should redeploy
        CHANGE_TO_REDEPLOY_DEMO: 20251215
