apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: probate-submit-service
  namespace: probate
spec:
  values:
    java:
      image: hmctsprod.azurecr.io/probate/submit-service:pr-1215-f808a68-20260324201251 #{"$imagepolicy": "flux-system:ithc-probate-submit-service"}
      environment:
        # Does what it says on the tin - if this changes then ithc should redeploy
        CHANGE_TO_REDEPLOY_ITHC: 20251215
