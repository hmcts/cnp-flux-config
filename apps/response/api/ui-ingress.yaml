---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rpe-response-api-ui
  namespace: response
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  rules:
    - host: response-api-ui.platform.hmcts.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rpe-response-api-nodejs
                port:
                  number: 80