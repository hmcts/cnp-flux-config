apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: github-arc
  namespace: flux-system
spec:
  path: ./apps/github-arc/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "github-arc"
      TEAM_NOTIFICATION_CHANNEL: "platops-build-notices"
