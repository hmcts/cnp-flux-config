apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bulk-scan-processor
spec:
  values:
    java:
      environment:
        OCR_VALIDATION_URL_SSCS: http://sscs-tribunals-api-aat.service.core-compute-aat.internal
