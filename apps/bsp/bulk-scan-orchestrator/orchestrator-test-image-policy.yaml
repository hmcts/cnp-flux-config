apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: bulk-scan-orchestrator-orchestrator-test
spec:
  imageRepositoryRef:
    name: bulk-scan-orchestrator-orchestrator-test
