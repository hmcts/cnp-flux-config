apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: bulk-scan-orchestrator-orchestrator-test
spec:
  imageRepositoryRef:
    name: bulk-scan-orchestrator-orchestrator-test
