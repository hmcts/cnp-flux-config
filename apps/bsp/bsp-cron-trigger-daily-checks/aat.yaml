apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bsp-cron-trigger-daily-checks
spec:
  releaseName: bsp-cron-trigger-daily-checks
  values:
    job:
      schedule: "0 9-18 * * *"
      environment:
        SEND_LETTER_SERVICE_URL: http://rpe-send-letter-service-aat.service.core-compute-aat.internal
        TRIGGER_TYPE: BSP_DAILY_CHECKS