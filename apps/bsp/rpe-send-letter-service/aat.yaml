apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rpe-send-letter-service
spec:
  values:
    java:
      environment:
        ENCRYPTION_ENABLED: false
        CIVIL_SERVICE_ENABLED: true
        DELETE_OLD_LETTERS_CRON: 0 0 8-18/2 ? * MON-FRI
        CIVIL_GENERAL_APPLICATIONS_INTERVAL: 3 days
        CIVIL_SERVICE_INTERVAL: 3 days
        CMC_CLAIM_STORE_INTERVAL: 3 days
        DIVORCE_FRONTEND_INTERVAL: 3 days
        FINREM_CASE_ORCHESTRATION_INTERVAL: 3 days
        FINREM_DOCUMENT_GENERATOR_INTERVAL: 3 days
        FPL_CASE_SERVICE_INTERVAL: 3 days
        NFDIV_CASE_API_INTERVAL: 3 days
        PRL_COS_API_INTERVAL: 3 days
        PROBATE_BACKEND_INTERVAL: 3 days
        SEND_LETTER_TESTS_INTERVAL: 3 days
        SSCS_INTERVAL: 3 days
