apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bulk-scan-payment-processor
spec:
  values:
    java:
      replicas: 0
      image: hmctspublic.azurecr.io/bulk-scan/payment-processor:pr-1219
      environment:
        REFRESH_PODS: "true"
