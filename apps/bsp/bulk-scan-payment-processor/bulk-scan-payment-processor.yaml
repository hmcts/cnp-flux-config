apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bulk-scan-payment-processor
spec:
  releaseName: bulk-scan-payment-processor
  values:
    java:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/bulk-scan/payment-processor:prod-07b5fa4-20260601075401 #{"$imagepolicy": "flux-system:bulk-scan-payment-processor"}
  chart:
    spec:
      chart: ./stable/bulk-scan-payment-processor
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
