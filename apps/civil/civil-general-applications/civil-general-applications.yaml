apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: civil-general-applications
  namespace: civil
spec:
  releaseName: civil-general-applications
  values:
    java:
      replicas: 0
      memoryRequests: "1536Mi"
      memoryLimits: "2048Mi"
      cpuRequests: "250m"
      cpuLimits: "1500m"
      autoscaling:
        enable: false
        maxReplicas: 0
        memory:
          averageUtilization: 125
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/civil/general-applications:prod-9af54b8-20260106135422 #{"$imagepolicy": "flux-system:civil-general-applications"}
      environment:
  chart:
    spec:
      chart: ./stable/civil-general-applications
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
