apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: civil-sdt
  namespace: civil
spec:
  values:
    java:
      environment:
        LOGGING_LEVEL_ORG_SPRINGFRAMEWORK: DEBUG
