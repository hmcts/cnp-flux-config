apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wa-workflow-api
spec:
  values:
    java:
      autoscaling:
        enabled: false
      replicas: 0
      environment:
        REFRESH_FLAG: ""
