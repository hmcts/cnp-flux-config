apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wa-reporting-frontend
spec:
  values:
    java:
      replicas: 3
      autoscaling:
        enabled: false
      memoryLimits: "2048Mi"
      memoryRequests: "2024Mi"
      cpuLimits: "2000m"
      cpuRequests: "1000m"
