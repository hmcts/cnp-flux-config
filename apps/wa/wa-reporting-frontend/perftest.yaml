apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wa-reporting-frontend
spec:
  values:
    nodejs:
      replicas: 2
      memoryLimits: "1024Mi"
      memoryRequests: "512Mi"
      cpuLimits: "1500m"
      cpuRequests: "750m"
