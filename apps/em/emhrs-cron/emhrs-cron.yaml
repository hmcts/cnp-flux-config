apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: emhrs-cron
spec:
  releaseName: emhrs-cron
  values:
    job:
      disableActiveClusterCheck: true
      image: hmctspublic.azurecr.io/em/hrs-api:prod-d540718-20250317191242 #{"$imagepolicy": "flux-system:em-hrs-api"}
      environment:
        TASK_NAME: UpdateJurisdictionCodesTask
      schedule: "0 0 31 2 *" #schedule of "0 0 31 2 *" is a mechanism to not run the job at all (31st feb is a nonfiring date)
      global:
        jobKind: CronJob
        enableKeyVaults: true
  chart:
    spec:
      chart: ./stable/emhrs-cron
      version: 0.0.2
      sourceRef:
        kind: HelmRepository
        name: hmcts-charts
        namespace: flux-system
