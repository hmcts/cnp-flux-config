apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: emhrs-cron
spec:
  releaseName: emhrs-cron
  values:
    job:
      memoryRequests: '1024Mi'
      memoryLimits: '2048Mi'
      cpuRequests: '500m'
      cpuLimits: '2500m'
      applicationPort: 8095
      # schedule: "15 20 * * *"
      environment:
        # IDAM_API_BASE_URI: "https://idam-api.platform.hmcts.net"
        # OPEN_ID_API_BASE_URI: "https://hmcts-access.service.gov.uk/o"
        # USE_AD_AUTH_FOR_SOURCE_BLOB_CONNECTION: true
        # TASK_NAME: UpdateTtlTask
        # UPDATE_TTL_BATCH_SIZE: 50
        # UPDATE_TTL_THREAD_LIMIT: 20
        # UPDATE_TTL_MAX_RECORDS: 1000
    global:
      environment: prod
