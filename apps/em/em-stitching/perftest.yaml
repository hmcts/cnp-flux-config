apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-stitching
spec:
  values:
    java:
      replicas: 1 # Needs to removed after testing
      memoryRequests: "4.5Gi"
      cpuRequests: "2000m"
      memoryLimits: "7Gi"
      cpuLimits: "4000m"
      image: hmctspublic.azurecr.io/em/stitching:pr-2148-7920fab-20240412081016 #{"$imagepolicy": "flux-system:perftest-em-stitching"}
      environment:
        CALLBACK_MAX_ATTEMPTS: 1
        TEST_VAR: refresh
        HISTORIC_DOCUMENT_TASKS_NO_OF_RETENTION_DAYS: 180
