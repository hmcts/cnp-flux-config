apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-stitching
spec:
  values:
    java:
      replicas: 1 # Needs to removed after testing
      memoryRequests: "4Gi"
      cpuRequests: "2000m"
      memoryLimits: "8Gi"
      cpuLimits: "4000m"
      image: hmctspublic.azurecr.io/em/stitching:prod-324d1e6-20240408105306 #{"$imagepolicy": "flux-system:perftest-em-stitching"}
      environment:
        CALLBACK_MAX_ATTEMPTS: 1
        TEST_VAR: refresh
        HISTORIC_DOCUMENT_TASKS_NO_OF_RETENTION_DAYS: 180
