apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: pr-656-em-stitching
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  filterTags:
    pattern: '^pr-656-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: ''
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: em-stitching
