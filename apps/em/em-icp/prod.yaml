apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-icp
spec:
  values:
    nodejs:
      environment:
        spotInstances:
          enabled: false
        IDAM_API_BASE_URL: https://idam-api.platform.hmcts.net
