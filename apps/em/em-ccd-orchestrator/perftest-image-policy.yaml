apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: perftest-em-ccd-orchestrator
  spec:
    filterTags:
      #      pattern: '^prod-[a-f0-9]+-(?P<ts>[0-9]+)'
      pattern: '^pr-1228-[a-f0-9]+-(?P<ts>[0-9]+)'
      extract: '$ts'
    policy:
      alphabetical:
        order: asc
spec:
  imageRepositoryRef:
    name: em-ccd-orchestrator
