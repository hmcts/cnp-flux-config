apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-ccd-orchestrator
spec:
  values:
    java:
      memoryRequests: '1.5Gi'
      autoscaling:
        enabled: true
        maxReplicas: 4
      environment:
        DUMMY: 1
