apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-ccd-orchestrator
spec:
  values:
    java:
      autoscaling:
        enabled: true
        maxReplicas: 4
      environment:
        DUMMY: 1
