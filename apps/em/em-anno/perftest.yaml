apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-anno
spec:
  values:
    java:
      memoryRequests: '1536Mi'
      autoscaling:
        enabled: true
        maxReplicas: 4
      environment:
        TEST_VAR: refresh
