apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-npa
spec:
  values:
    java:
      memoryRequests: '1536Mi'
      memoryLimits: '3072Mi'
      environment:
        TEST_VAR: refresh
