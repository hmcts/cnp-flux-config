apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-npa
spec:
  values:
    java:
      memoryRequests: "1.5Gi"
      memoryLimits: "3Gi" # Was 2Gi
      environment:
        TEST_VAR: refresh
