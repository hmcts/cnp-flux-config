apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-npa
spec:
  values:
    java:
      memoryRequests: '2048Mi'
      memoryLimits: '3072Mi'
      autoscaling:
        enabled: true
        maxReplicas: 4 # On each cluster
      environment:
        TEST_VAR: refresh
