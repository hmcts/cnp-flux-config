apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: em-npa
spec:
  values:
    java:
      environment:
        TEST_VAR: value1
        ENABLE_SEARCH_MARKUPS_ENDPOINT: true
