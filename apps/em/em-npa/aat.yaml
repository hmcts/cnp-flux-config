apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: em-npa
spec:
  values:
    java:
      environment:
        TEST_VAR: refresh
        S2S_NAMES_DELETE_WHITELIST: em_gw
        ENABLE_DELETE_ENDPOINT: true
