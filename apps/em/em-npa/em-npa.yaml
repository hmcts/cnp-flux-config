apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: em-npa
spec:
  releaseName: em-npa
  values:
    java:
      useInterpodAntiAffinity: true
      replicas: 2 # minReplicas will default to this value
      autoscaling:
        enabled: false
      image: hmctspublic.azurecr.io/em/npa:prod-4763450-20240415104250 #{"$imagepolicy": "flux-system:em-npa"}
  chart:
    spec:
      chart: ./stable/em-npa
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
