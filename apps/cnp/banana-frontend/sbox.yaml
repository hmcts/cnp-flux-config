apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: banana-frontend
spec:
  values:
    nodejs:
      ingressHost: banana.sandbox.platform.hmcts.net
      replicas: 2
      autoscaling:
        minReplicas: 1
