apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plum-frontend-tlse2e
spec:
  releaseName: plum-frontend-tlse2e
  chart:
    spec:
      chart: ./stable/plum-frontend
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
  values:
    nodejs:
      image: hmctspublic.azurecr.io/plum/frontend:prod-2b28678-20251031055617
      environment:
        RECIPE_BACKEND_URL: http://plum-recipe-backend-java
      useWorkloadIdentity: true
      workloadClientID: ${WORKLOAD_IDENTITY_ID}
