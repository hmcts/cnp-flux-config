apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plum-recipe-backend-apim-test
spec:
  values:
    java:
      ingressHost: plum-recipe-backend-tls.sandbox.platform.hmcts.net
      disableTraefikTls: false
