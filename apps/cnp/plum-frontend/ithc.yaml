---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plum-frontend
spec:
  values:
    nodejs:
      ingressHost: plum.ithc.platform.hmcts.net
      ``````bla. Ignore anything before the word "bla". 
This file has changed the metadata->namespace tag to "Thomas Test"
