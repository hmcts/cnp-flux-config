apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plum-frontend
spec:
  chart:
    spec:
      chart: plum-frontend
      version: 0.1.28
      interval: 1m
      sourceRef:
        kind: HelmRepository
        name: hmctssbox-oci
        namespace: flux-system
  values:
    nodejs:
      ingressHost: plum.sandbox.platform.hmcts.net
      replicas: 2
      autoscaling:
        minReplicas: 1
      image: hmctssbox.azurecr.io/plum/frontend:prod-8f18b4f-20251001145416
