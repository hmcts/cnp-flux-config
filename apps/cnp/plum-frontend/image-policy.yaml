apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: plum-frontend
  annotations:
    hmcts.github.com/image-registry: hmctsprod
spec:
  imageRepositoryRef:
    name: plum-frontend
