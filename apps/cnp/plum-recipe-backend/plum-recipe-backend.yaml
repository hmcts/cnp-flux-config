apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plum-recipe-backend
spec:
  releaseName: plum-recipe-backend
  values:
    java:
      image: hmctspublic.azurecr.io/plum/recipe-backend:prod-51d4ea3-20231002095334 # {"$imagepolicy": "flux-system:plum-recipe-backend"}
      useWorkloadIdentity: true
      workloadClientID: ${WORKLOAD_IDENTITY_ID}
  chart:
    spec:
      chart: ./stable/plum-recipe-backend
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
