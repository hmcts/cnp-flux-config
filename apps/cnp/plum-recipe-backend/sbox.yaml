apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plum-recipe-backend
  annotations:
    hmcts.github.com/java-tool-options-patch: enabled
spec:
  values:
    java:
      ingressHost: plum-recipe-backend-sandbox.service.core-compute-sandbox.internal