apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dmstore-cron
spec:
  releaseName: dmstore-cron
  values:
    job:
      applicationPort: 8095
    global:
      environment: demo
