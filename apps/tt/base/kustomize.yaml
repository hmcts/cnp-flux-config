apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: tt
  namespace: flux-system
spec:
  path: ./apps/tt/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "tt"
      TEAM_NOTIFICATION_CHANNEL: "pet"
      TEAM_AAD_GROUP_ID: "dcd_group_pet_v2"
