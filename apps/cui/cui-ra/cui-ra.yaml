apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: cui-ra
spec:
  releaseName: cui-ra
  values:
    nodejs:
      image: hmctspublic.azurecr.io/cui/ra:prod-d6f0bc1-20240123101225 #{"$imagepolicy": "flux-system:cui-ra"}
  chart:
    spec:
      chart: ./stable/cui-ra
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
