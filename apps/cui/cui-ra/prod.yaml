apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cui-ra
spec:
  values:
    nodejs:
      ingressHost: www.manage-your-support-for-hmcts-services.service.gov.uk
      environment:
        DEMO_ENABLED: 'false'
