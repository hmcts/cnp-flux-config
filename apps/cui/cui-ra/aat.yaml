apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cui-ra
spec:
  values:
    nodejs:
      spotInstances:
        enabled: false
      environment:
        DEMO_ENABLED: 'true'
