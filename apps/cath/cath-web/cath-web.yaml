apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-web
spec:
  releaseName: cath-web
  interval: 5m
  values:
    nodejs:
      releaseNameOverride: cath-web
      image: hmctsprod.azurecr.io/cath/cath-web:prod-a8810dc-20260904162819 # {"$imagepolicy": "flux-system:cath-web"}
  chart:
    spec:
      chart: cath-web
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci
        namespace: flux-system
      interval: 1m
