apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-postgres
spec:
  releaseName: cath-postgres
  interval: 5m
  values:
    nodejs:
      releaseNameOverride: cath-postgres
      image: hmctsprod.azurecr.io/cath/cath-postgres:prod-50dd8da-20260904154029 # {"$imagepolicy": "flux-system:cath-postgres"}
  chart:
    spec:
      chart: cath-postgres
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci
        namespace: flux-system
      interval: 1m
