apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer
  namespace: ccd
spec:
  releaseName: ccd-logstash-indexer
  values:
    replicas: 0
    pdb:
      enabled: false
