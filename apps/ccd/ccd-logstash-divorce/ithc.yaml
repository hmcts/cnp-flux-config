apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-divorce
  namespace: ccd
spec:
  releaseName: ccd-logstash-divorce
  values:
    replicas: 1
    imageTag: "9.1.2"
  chart:
    spec:
      chart: logstash
      version: 9.1.2
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: ccd
