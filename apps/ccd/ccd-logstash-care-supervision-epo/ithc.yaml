apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-care-supervision-spo
  namespace: ccd
spec:
  releaseName: ccd-logstash-care-supervision-spo
  values:
    replicas: 0
