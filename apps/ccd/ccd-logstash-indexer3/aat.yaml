apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer3
  namespace: ccd
spec:
  releaseName: ccd-logstash-indexer3
  values:
    replicas: 0
    pdb:
      enabled: false