# Use ccd-case-disposer, this was introduced for testing purposes
# and is not used in production.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-case-disposer-v2
spec:
  releaseName: ccd-case-disposer-v2
  values:
    job:
      suspend: true
      useInterpodAntiAffinity: true
      image: hmctsprod.azurecr.io/ccd/case-disposer:prod-2d8855e-20260824135050 #{"$imagepolicy": "flux-system:ccd-case-disposer"}
  chart:
    spec:
      chart: ./stable/ccd-case-disposer
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
