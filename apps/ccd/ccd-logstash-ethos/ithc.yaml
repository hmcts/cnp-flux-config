apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-ethos
  namespace: ccd
spec:
  releaseName: ccd-logstash-ethos
  values:
    replicas: 1
    imageTag: "9.1.2"
  chart:
    spec:
      chart: logstash
      version: 9.1.2
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: ccd
