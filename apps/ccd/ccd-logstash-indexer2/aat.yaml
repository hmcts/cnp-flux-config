apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer2
  namespace: ccd
spec:
  releaseName: ccd-logstash-indexer2
  values:
    replicas: 0
    pdb:
      enabled: false