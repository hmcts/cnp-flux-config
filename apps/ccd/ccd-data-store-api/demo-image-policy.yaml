apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: demo-ccd-data-store-api
spec:
  imageRepositoryRef:
    name: ccd-data-store-api
  filterTags:
    pattern: '^pr-2321-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
