apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer4
  namespace: ccd
spec:
  releaseName: ccd-logstash-indexer4
  values:
    replicas: 0
    pdb:
        enabled: false