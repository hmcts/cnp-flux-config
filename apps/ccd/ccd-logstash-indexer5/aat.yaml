apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ccd-logstash-indexer5
  namespace: ccd
spec:
  releaseName: ccd-logstash-indexer5
  values:
    replicas: 0
    pdb:
        enabled: false