apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: perftest-ccd-definition-store-api
  annotations:
    hmcts.github.com/prod-automated: enabled
spec:
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: ccd-definition-store-api
