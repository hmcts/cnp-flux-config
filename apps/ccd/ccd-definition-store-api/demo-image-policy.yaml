apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: demo-ccd-definition-store-api
spec:
  filterTags:
    pattern: '^pr-1405-[a-f0-9]+-(?P<ts>[0-9]+)'
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: ccd-definition-store-api
