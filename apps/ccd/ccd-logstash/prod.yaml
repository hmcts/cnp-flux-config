apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
labels:
  logstash-instance: ccd-logstash-prod # Workaround for upstream kustomize bug - https://github.com/kubernetes-sigs/kustomize/issues/6031
metadata:
  name: ccd-logstash
  namespace: ccd
spec:
  releaseName: ccd-logstash
  values:
    replicas: 1
