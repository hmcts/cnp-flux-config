apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: demo-prl-ccd-case-migration
spec:
  imageRepositoryRef:
    name: prl-ccd-case-migration
  filterTags:
    pattern: '^pr-31-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    alphabetical:
      order: asc