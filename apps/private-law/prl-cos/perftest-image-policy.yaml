apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: perftest-prl-cos
spec:
  imageRepositoryRef:
    name: prl-cos
    filterTags:
      pattern: '^pr-3030-[a-f0-9]+-(?P<ts>[0-9]+)'
      extract: '$ts'
    policy:
      alphabetical:
        order: asc
