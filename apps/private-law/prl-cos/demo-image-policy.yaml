apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: demo-prl-cos
spec:
  imageRepositoryRef:
    name: prl-cos
  filterTags:
    pattern: "^pr-3428-[a-f0-9]+-(?P<ts>[0-9]+)"
    extract: "$ts"
