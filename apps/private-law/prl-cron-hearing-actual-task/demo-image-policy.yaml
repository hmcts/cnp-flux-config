apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: demo-prl-cron-hearing-actual-task
spec:
  imageRepositoryRef:
    name: prl-cos
  filterTags:
    pattern: "^pr-3810-[a-f0-9]+-(?P<ts>[0-9]+)"
    extract: "$ts"
  policy:
    alphabetical:
      order: asc
