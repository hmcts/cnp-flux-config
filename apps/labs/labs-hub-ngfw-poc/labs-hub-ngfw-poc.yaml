---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: labs-hub-ngfw-poc
  namespace: labs
spec:
  releaseName: labs-hub-ngfw-poc
  chart:
    spec:
      chart: ./stable/labs-hub-ngfw-poc
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
  values:
    nodejs:
      image: hmctssandbox.azurecr.io/labs/hub-ngfw-poc:latest # {"$imagepolicy": "flux-system:labs-hub-ngfw-poc"}
      disableTraefikTls: true
