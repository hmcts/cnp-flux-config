---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: labs-ozzyib-nodejs
  namespace: labs
spec:
  releaseName: labs-ozzyib-nodejs
  chart:
    spec:
      chart: ./stable/labs-ozzyib-nodejs
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
  values:
    nodejs:
      image: hmctssandbox.azurecr.io/labs/ozzyib-nodejs:latest # {"$imagepolicy": "flux-system:labs-ozzyib-nodejs"}
      disableTraefikTls: true
