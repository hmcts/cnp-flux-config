apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: labs-zchmcts
  namespace: labs
spec:
  releaseName: labs-zchmcts
  chart:
    spec:
      chart: ./stable/labs-zchmcts
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
  values:
    nodejs:
      image: hmctssandbox.azurecr.io/labs/zchmcts:prod-ea01e1a-20220224173053 # {"$imagepolicy": "flux-system:labs-zchmcts"}
      disableTraefikTls: true
