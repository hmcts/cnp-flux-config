---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: labs-MartyFox
  namespace: labs
spec:
  releaseName: labs-MartyFox
  chart:
    spec:
      chart: ./stable/labs-MartyFox
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
  values:
    nodejs:
      image: hmctssandbox.azurecr.io/labs/MartyFox:latest # {"$imagepolicy": "flux-system:labs-MartyFox"}
      disableTraefikTls: true
