apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: neuvector
  namespace: neuvector
spec:
  chart:
    spec:
      chart: crd
      version: x.x.x
      sourceRef:
        kind: HelmRepository
        name: neuvector-helm
        namespace: flux-system