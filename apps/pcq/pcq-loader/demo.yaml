apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pcq-loader
spec:
  values:
    job:
      image: 'hmctspublic.azurecr.io/pcq/loader:latest' #{"$imagepolicy": "flux-system:demo-pcq-loader"}
      schedule: "0/15 * * * *"
      environment:
        PCQ_BACKEND_URL: http://pcq-backend-demo.service.core-compute-demo.internal
        PCQ_LOADER_THROW_EXCEPTION: true