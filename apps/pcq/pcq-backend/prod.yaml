apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: pcq-backend
spec:
  values:
    java:
      spotInstances:
        enabled: false
      autoscaling:
        minReplicas: 2
        maxReplicas: 3
