apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: pcq-frontend
spec:
  values:
    nodejs:
      ingressHost: equality-and-diversity.platform.hmcts.net
      environment:
        DUMMY_RESTART_VAR: 1
        NODE_ENV: "production"
