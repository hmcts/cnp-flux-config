apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pcq-frontend
spec:
  values:
    nodejs:
      ingressHost: pcq.aat.platform.hmcts.net
      startupDelay: 30
      environment:
        ENABLE_INVOKER: true
