apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: rd-profile-sync
spec:
  releaseName: rd-profile-sync
  values:
    java:
      autoscaling:
        enabled: true
        maxReplicas: 2
      memoryRequests: '2048Mi'
      memoryLimits: '2048Mi'
      replicas: 1
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/rd/profile-sync:prod-cbf54e0-20240321100436 #{"$imagepolicy": "flux-system:rd-profile-sync"}
      environment:
        DUMMY_VAR: true
  chart:
    spec:
      chart: ./stable/rd-profile-sync
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
