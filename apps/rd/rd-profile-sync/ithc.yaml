apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rd-profile-sync
spec:
  values:
    java:
      replicas: 0
      autoscaling:
        enabled: true
        maxReplicas: 0
      environment:
        DELETE_ORG: false