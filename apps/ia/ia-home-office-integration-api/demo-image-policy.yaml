apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: demo-ia-home-office-integration-api # comment to re-trigger the build
spec:
  imageRepositoryRef:
    name: ia-home-office-integration-api
