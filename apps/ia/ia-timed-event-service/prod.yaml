apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ia-timed-event-service
spec:
  values:
    java:
      environment:
        IDAM_URL: "https://hmcts-access.service.gov.uk"
        POSTGRES_HOST: ia-timed-event-service-postgres-db-v15-prod.postgres.database.azure.com
        POSTGRES_USERNAME: pgadmin
        RESTART_PODS: "true"
