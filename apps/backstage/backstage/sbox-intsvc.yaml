apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
  namespace: backstage
spec:
  values:
    ingress:
      host: backstage-sandbox.hmcts.net
    backend:
      image: hmctspublic.azurecr.io/backstage-portal/backend:pr-49-5aabf5ef-1758890573 # {"$imagepolicy": "flux-system:ptlsbox-backstage-portal-backend"}
      db:
        user: pgadmin
        host: hmcts-backstage-ptlsbox-restore.postgres.database.azure.com
        ssl: require
      cors:
        origin: https://backstage-sandbox.hmcts.net
    techdocs:
      storageUrl: https://backstage-api.sandbox.platform.hmcts.net/api/techdocs/static/docs
      requestUrl: http://backstage-api.sandbox.platform.hmcts.net/api/techdocs
