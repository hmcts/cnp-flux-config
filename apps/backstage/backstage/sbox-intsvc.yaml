apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
  namespace: backstage
spec:
  values:
    ingress:
      host: backstage-sandbox.hmcts.net
    backend:
      image: hmctspublic.azurecr.io/backstage-portal/backend:pr-28-6a337f8a-1758103097 # {"$imagepolicy": "flux-system:ptlsbox-backstage-portal-backend"}
      db:
        user: pgadmin
        host: hmcts-backstage-ptlsbox.postgres.database.azure.com
        ssl: require
      cors:
        origin: https://backstage-sandbox.hmcts.net
    jenkins:
      cftUsername: praveen.adusumilli@hmcts.net
    techdocs:
      storageUrl: https://backstage-api.sandbox.platform.hmcts.net/api/techdocs/static/docs
      requestUrl: http://backstage-api.sandbox.platform.hmcts.net/api/techdocs
