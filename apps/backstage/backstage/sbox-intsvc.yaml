apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
  namespace: backstage
spec:
  values:
    ingress:
      host: backstage.sandbox.platform.hmcts.net
    backend:
      image: hmctspublic.azurecr.io/backstage-portal/backend:prod-770ae12f-1757580402 # {"$imagepolicy": "flux-system:ptlsbox-backstage-portal-backend"}
      db:
        user: pgadmin
        host: hmcts-backstage-ptlsbox.postgres.database.azure.com
        ssl: require
      cors:
        origin: https://backstage.sandbox.platform.hmcts.net
    jenkins:
      cftUsername: praveen.adusumilli@hmcts.net
    techdocs:
      storageUrl: https://backstage-api.sandbox.platform.hmcts.net/api/techdocs/static/docs
      requestUrl: http://backstage-api.sandbox.platform.hmcts.net/api/techdocs
