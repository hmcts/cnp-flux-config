apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: aat-lau-frontend
spec:
  filterTags:
    pattern: "^staging-[a-f0-9]+-(?P<ts>[0-9]+)"
    extract: "$ts"
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: lau-frontend
