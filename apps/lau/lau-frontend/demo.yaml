apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lau-frontend
spec:
  values:
    nodejs:
      environment:
        OTEL_SDK_DISABLED: true
      image: 'hmctspublic.azurecr.io/lau/frontend:latest' #{"$imagepolicy": "flux-system:demo-lau-frontend"}
