apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dynatrace-injection-monitor
  namespace: monitoring
  labels:
    app: dynatrace-injection-monitor
spec:
  values:
    environment:
      ENVIRONMENT: sbox
      CLUSTER_NAME: cft-sbox-00-aks
      DRY_RUN: true
      ADDITIONAL_EXCLUDED_NAMESPACES: "thomas-namespace"
