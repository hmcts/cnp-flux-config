apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: botkube
  namespace: flux-system
spec:
  path: ./apps/botkube/${ENVIRONMENT}/base
  postBuild:
    substitute:
      NAMESPACE: "monitoring"
      ENV_INJECTOR: "disabled"
