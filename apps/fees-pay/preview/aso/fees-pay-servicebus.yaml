apiVersion: servicebus.azure.com/v1api20210101preview
kind: Namespace
metadata:
  name: ${NAMESPACE}-servicebus-${ENVIRONMENT}
  namespace: ${NAMESPACE}
spec:
  tags:
    app.kubernetes.io_name: fees-and-payments
    application: fees-and-payments
  sku:
    name: Standard