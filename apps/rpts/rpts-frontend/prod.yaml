apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rpts-frontend
spec:
  values:
    nodejs:
      #      ingressHost: www.residential-property-tribunal.gov.uk - to add in once we have the domain set up
      environment:
        FRONTEND_URL: todo
