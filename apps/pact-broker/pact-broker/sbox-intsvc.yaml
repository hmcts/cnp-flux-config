apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pact-broker
  namespace: pact-broker
chart:
  spec:
    chart: pact-broker
      version: 0.10.0
      sourceRef:
        kind: HelmRepository
        name: hmctspublic
        namespace: flux-system
