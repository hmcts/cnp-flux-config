apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: preview-finrem-citizen-ui
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  filterTags:
    pattern: '^preview-[a-f0-9]+-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: finrem-citizen-ui
