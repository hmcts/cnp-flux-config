apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: finrem-cos
  namespace: financial-remedy
spec:
  values:
    java:
      memoryRequests: "1280Mi"
      autoscaling:
        cpu:
          enabled: false
      environment:
        FEATURE_PBA_CASE_TYPE: false
        VAR_FLUX_V2: trigger3
