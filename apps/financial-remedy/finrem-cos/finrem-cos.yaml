apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: finrem-cos
  namespace: financial-remedy
spec:
  releaseName: finrem-cos
  values:
    java:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/finrem/cos:prod-8f55961-20240429094632 #{"$imagepolicy": "flux-system:finrem-cos"}
      environment:
        BSP_SERVICES_ALLOWED_TO_UPDATE: bulk_scan_orchestrator
        BSP_SERVICES_ALLOWED_TO_VALIDATE: bulk_scan_processor
        CTSC_OPENING_HOURS: 8am to 6pm, Monday to Friday
        VAR_TA: trigger
        FEATURE_RESPONDENT_JOURNEY: true
        FEATURE_MANAGE_BUNDLE: true
  chart:
    spec:
      chart: ./stable/finrem-cos
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
