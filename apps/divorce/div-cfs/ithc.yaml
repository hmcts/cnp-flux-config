apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: div-cfs
  namespace: divorce
spec:
  values:
    java:
      image: hmctsprod.azurecr.io/div/cfs:prod-94c8c80-20260326110044 #{"$imagepolicy": "flux-system:div-cfs"}
      environment:
        IDAM_API_BASEURL: "https://idam-api.ithc.platform.hmcts.net"
        VAR_FV2: ithc1
      prometheus:
        enabled: true
      autoscaling:
        enabled: true
        minReplicas: 1
        maxReplicas: 2
      cpuRequests: "250m"
      memoryRequests: "1536Mi"
