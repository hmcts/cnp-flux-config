apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: div-cms
  namespace: divorce
spec:
  values:
    java:
      image: hmctspublic.azurecr.io/div/cms:pr-782-a06ce32-20250430103420 #{"$imagepolicy": "flux-system:demo-div-cms"}
      environment:
        IDAM_API_BASEURL: "https://idam-api.demo.platform.hmcts.net"
        TRIGGER_DEPLOY: trigger
      aadIdentityName: divorce
      prometheus:
        enabled: true
