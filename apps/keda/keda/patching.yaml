---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keda
  namespace: keda
spec:
  values:
    serviceAccount:
      operator:
        create: false
        name: keda
      metricServer:
        create: false
        name: keda
      webhooks:
        create: false
        name: keda
  chart:
    spec:
      chart: keda
      version: 2.14.3
