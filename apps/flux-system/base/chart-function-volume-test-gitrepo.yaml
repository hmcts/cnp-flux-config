apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: chart-function-volume-test
  namespace: flux-system
spec:
  interval: 5m
  url: ssh://github.com/hmcts/chart-function
  ref:
    branch: step-through-library-for-breaking-change
  secretRef:
    name: git-credentials
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/function/