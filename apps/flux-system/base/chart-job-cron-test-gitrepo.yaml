apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: chart-job-cron-test
  namespace: flux-system
spec:
  interval: 5m
  url: ssh://git@github.com/hmcts/chart-job
  ref:
    branch: "2.1.1"
  secretRef:
    name: git-credentials
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/job/