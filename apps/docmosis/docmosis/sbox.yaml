apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: docmosis
spec:
  values:
    ingressHost: docmosis.sandbox.platform.hmcts.net
    image: hmctsprod.azurecr.io/docmosis:sandbox-e5efc01-1012955 #{"$imagepolicy": "flux-system:sandbox-docmosis"}
    environment:
      DOCMOSIS_RENDER_USEURL: https://docmosis.sandbox.platform.hmcts.net
      DOCMOSIS_TORNADO_RENDER_USEURL: https://docmosis.sandbox.platform.hmcts.net
  chart:
    spec:
      chart: base
      version: 1.2.0
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci
        namespace: flux-system
