apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: et-ccd-callbacks
spec:
  releaseName: et-ccd-callbacks
  values:
    java:
      replicas: 2
      useInterpodAntiAffinity: true
      image: hmctspublic.azurecr.io/et/ccd-ballbacks:latest #{"$imagepolicy": "flux-system:et-ccd-callbacks"}
  chart:
    spec:
      chart: ./stable/et-ccd-callbacks
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m
