apiVersion: servicebus.azure.com/v1api20210101preview
kind: Namespace
metadata:
  name: ${NAMESPACE}-servicebus-${ENVIRONMENT}
  namespace: ${NAMESPACE}
spec:
  tags:
    app.kubernetes.io_name: employment-tribunals
    application: employment-tribunals
  sku:
    name: Standard