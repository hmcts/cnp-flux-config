apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ccd-export
spec:
  values:
    base:
      environment:
        CCD_IDAM_BASE_URL: "https://idam-api.platform.hmcts.net"
