apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: admin
spec:
  values:
    base:
      environment:
        CCD_UI_BASE_URL: "https://manage-case.ithc.platform.hmcts.net/case/EMPLOYMENT"
